<svg width="600" height="160" viewBox="0 0 600 160" fill="none" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <linearGradient id="grad2" x1="0" y1="0" x2="600" y2="0" gradientUnits="userSpaceOnUse">
      <stop offset="0%" stop-color="#7b2cbf"/>
      <stop offset="100%" stop-color="#00d4ff"/>
    </linearGradient>
  </defs>

  <!-- Dark background for preview -->
  <rect width="600" height="160" rx="12" fill="#0d0d0d"/>

  <!-- Accent bar -->
  <rect x="30" y="30" width="4" height="100" rx="2" fill="url(#grad2)"/>

  <!-- "tiago" light -->
  <text x="54" y="88" font-family="'Space Grotesk', system-ui, sans-serif" font-weight="300" font-size="52" fill="white" letter-spacing="-1">tiago</text>

  <!-- "borges" bold gradient -->
  <text x="258" y="88" font-family="'Space Grotesk', system-ui, sans-serif" font-weight="700" font-size="52" fill="url(#grad2)" letter-spacing="-1">borges</text>

  <!-- Subtitle -->
  <text x="56" y="118" font-family="'Inter', system-ui, sans-serif" font-weight="400" font-size="13" fill="#666" letter-spacing="4">AUTOMAÇÕES &amp; DESENVOLVIMENTO</text>
</svg>
